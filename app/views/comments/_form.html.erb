<%= form_with(model: [commentable, Comment.new], local: true) do |f| %>
  <div>
    <%= f.label :body, 'コメント' %>
    <%= f.text_area :body, required: true %>
  </div>
  <%= f.submit 'コメントする' %>
<% end %>
